<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width">
    <title>{{ film.title }} - Détails</title>
</head>
<body>
    <h1>{{ film.title }}</h1>

    <p><strong>Année de sortie:</strong> {{ film.year }}</p>
    <p><strong>Genre:</strong> {{ film.type }}</p>
    <p><strong>Réalisateur:</strong> {{ film.director }}</p>
    <p><strong>Synopsis:</strong> {{ film.synopsis }}</p>
    
    <p><strong>Créé le:</strong> {{ film.createdAt|date('d/m/Y') }}</p>
    <p><strong>Mis à jour le:</strong> {{ film.updatedAt|date('d/m/Y') }}</p>

    <a href="/films">Retour à la liste des films</a>
</body>
</html>
