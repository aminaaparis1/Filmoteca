<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width">
    <title>{{ title }}</title>
    <style>
        /* Style du tableau */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        /* Style du titre de la page */
        h1 {
            font-family: Arial, sans-serif;
            color: #333;
            text-align: center;
            margin-top: 20px;
        }

        /* Style pour le message d'absence de films */
        p {
            font-size: 1.2em;
            text-align: center;
            color: #666;
        }
    </style>
</head>
<body>
    <h1>{{ title }}</h1>

    <!-- Vérifier si la liste des films est vide -->
    {% if films is empty %}
        <p>Aucun film à afficher.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Année</th>
                    <th>Type</th>
                    <th>Synopsis</th>
                    <th>Réalisateur</th>
                </tr>
            </thead>
            <tbody>
                <!-- Boucle à travers les films et les afficher dans le tableau -->
                {% for film in films %}
                    <tr>
                        <td>{{ film.id }}</td>
                        <td>{{ film.title }}</td>
                        <td>{{ film.year }}</td>
                        <td>{{ film.type }}</td>
                        <td>{{ film.synopsis }}</td>
                        <td>{{ film.director }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</body>
</html>
